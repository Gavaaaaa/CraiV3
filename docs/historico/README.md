# Histórico

Esta pasta guarda material encerrado do repositório antigo: auditorias, achados, a aprovação do Sprint 3, o baseline do pytest e a demo antiga.
Ficaram só no repositório antigo, `Gavaaaaa/Crai`, os protótipos pré-unificação da pasta `archive/` (módulos 02 autoencoder, 03 payday e 04 offer bandit, com relatórios e PNGs).
Ficaram só no `Gavaaaaa/mvp-crai` o dashboard em JavaScript e os encoders JS dos modelos (`data/encoders/`); dele vieram para cá apenas a base de exemplo e a seção de limitações.
Os dois repositórios foram arquivados no GitHub, não deletados: o conteúdo continua acessível, somente leitura.
O que foi apagado, movido e trazido na migração está registrado em `../MIGRACAO_FEITA.md`.
